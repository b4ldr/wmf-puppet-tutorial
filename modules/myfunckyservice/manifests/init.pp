# This name needs to match the module name i.e. the folder that exists under modules
# init.pp is a special file which will always be used when some one includes or instantiates a class e.g.
# class {'myfunckyservice'} or include myfunckyservice
class myfunckyservice (
) {

  # Install the applications
  # write our the config file
  # manage the service
}
